# Play Assist
An AI based media content suggestion application.

## Frontend
The frontend uses the React library.

## Backend
The backend uses python based FastAPI as backend sever for apis. And PostgreSQL via SQLAlchemy for storing vectors to utilize it for building RAG. 